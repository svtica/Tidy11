# Drop README screenshots here

Expected files:

- `tidy11-main.png` — main GUI window with the SAFE preset selected
- `tidy11-verify.png` — output of the Verify (read-only) button

Recommended capture: 900x900 window at default Win11 dark theme, taken on a fresh
checkout so the layout matches the in-repo XAML.

Once committed, the README screenshot table at the top will render automatically.
